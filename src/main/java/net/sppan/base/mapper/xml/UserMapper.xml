<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.sppan.base.mapper.UserMapper">
	<!--开启二级缓存-->
	<cache type="org.mybatis.caches.ehcache.LoggingEhcache"/>

	<resultMap id="BaseResultMap" type="net.sppan.base.entity.User">
		<id column="id" property="id" />
		<result column="user_name" property="userName" />
		<result column="nick_name" property="nickName" />
		<result column="password" property="password" />
		<result column="sex" property="sex" />
		<result column="birthday" property="birthday" />
		<result column="telephone" property="telephone" />
		<result column="email" property="email" />
		<result column="address" property="address" />
		<result column="delete_status" property="deleteStatus" />
		<result column="locked" property="locked" />
		<result column="description" property="description" />
		<result column="create_time" property="createTime" />
		<result column="update_time" property="updateTime" />
	</resultMap>

    <!-- 通用查询结果列-->
    <sql id="Base_Column_List">
        id, user_name AS userName, nick_name AS nickName, password, sex, birthday, telephone, email, address, delete_status AS deleteStatus, locked, description, create_time AS createTime, update_time AS updateTime
    </sql>
    
    <select id="selectResource" parameterType="string" resultType="string">
    	select 
		  source_key 
		from
		  tb_resource as r,
		  tb_resources_role as rr,
		  tb_role as tr,
		  tb_role_user ru,
		  tb_user u 
		where u.user_name = #{username} 
		  and u.id = ru.u_id
		  and ru.r_id = tr.id
		  and tr.id = rr.r_id
		  and rr.s_id = r.id
    </select>
</mapper>
